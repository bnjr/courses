const today = new Date();

const person = {
  age: 20
}

class Color {};

const red = new Color();

let someArray = ['red', 1];

const add = (a: number, b: number): number => {
  return a + b;
}

console.log(add(4, 5))